{
  "name": "comedical-scout-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.js",
    "build:an": "node scripts/build.js --company=an-visiting-nurse",
    "build:chigasaki": "node scripts/build.js --company=chigasaki-tokushukai",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.11",
    "vitest": "^2.1.9"
  }
}
